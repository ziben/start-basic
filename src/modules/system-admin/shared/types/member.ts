export type AdminMemberInfo = {
  id: string
  userId: string
  username: string
  email: string
  organizationId: string
  organizationName: string
  organizationSlug: string
  role: string
  createdAt: string
}
