import { Button } from '@/components/ui/button'
import { Settings2 } from 'lucide-react'

interface RoleNavGroupPrimaryButtonsProps {
    // 预留未来可能的操作按钮
}

export function RoleNavGroupPrimaryButtons({ }: RoleNavGroupPrimaryButtonsProps) {
    // 目前这个页面没有主要操作按钮，预留组件结构
    return null
}
